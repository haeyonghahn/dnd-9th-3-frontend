import { atom } from "recoil";

export const deleteRecordPopUpOverAtom = atom({
  key: "deleteRecordPopUpOver",
  default: false,
});

export const chooseTimeLineMonthAtom = atom({
  key: "chooseTimeLineMonth",
  default: false,
});
